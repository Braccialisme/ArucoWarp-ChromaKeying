[project]
name = "detourage"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "opencv-python",
    "numpy",
    "Pillow",
    "numba",
]

[project.optional-dependencies]
rembg = ["rembg"]